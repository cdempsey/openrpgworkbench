{
  "name": "@openrpgworkbench/app",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "bootstrap": "npm install && lerna bootstrap --hoist"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=7.0.0"
  }
}
